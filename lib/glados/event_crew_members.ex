defmodule Glados.EventCrewMembers do
  @moduledoc """
  Defines logic for handling crew members per event.
  """

  @doc """
  Creates the initial relationship between a user and an event.

  """
  def send_application(user_id, event_id, answers) do
  end
end
